var person = {
    name: "Bedirhan",
    age: "27",
    city: "Istanbul",
}

console.log(person)

console.log(person.name)

// console.log("Hos geldin " + person.name + ", " + person.city)

// Yorum satırı

var professionalPrint = `Hos geldin ${person.name} ${person.city}`

// var let const

